;; -*- lexical-binding: t; -*-
;; -------------------------------------------------------------------------------------------------
;; Configuration
;; -------------------------------------------------------------------------------------------------

;; Reopen files at the last visited position
(save-place-mode 1)

;; Keep minibuffer history across restarts
(savehist-mode 1)
