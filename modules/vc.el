;; -*- lexical-binding: t; -*-
;; -------------------------------------------------------------------------------------------------
;; Configuration
;; -------------------------------------------------------------------------------------------------

;; Follow without asking, instead of prompting for every symlinked file
(setq vc-follow-symlinks t)
